##spring config
## DB Config

spring:
 shardingsphere:
  datasource:
   common:
    type: com.zaxxer.hikari.HikariDataSource
   names: ds0
   ds0:
    driver-class-name: com.mysql.cj.jdbc.Driver
    jdbc-url: jdbc:mysql://localhost:3306/test?useUnicode=true&characterEncoding=UTF-8&useSSL=false&serverTimezone=GMT%2B8
    uesrname: root
    password: cheney@mysql
  sharding:
   tables:
    sys_auth_user:
     actual-data-nodes: ds.sys_auth_user
     table-strategy:
      standard:
       sharding-column: create_date
       precise-algorithm-class-name: com.cheney.utils.YearMonthShardingAlgorithm
#     databaseStrategy:
#      standard:
#       shardingColumn: create_date
#       shardingAlgorithmName: year-month
#   sharding-algorithms:
#    year-month:
#     strategy: STANDARD
#     precise-algorithm-class-name: com.cheney.utils.YearMonthShardingAlgorithm




 ## kafka
 kafka:
  bootstrap-servers: '192.168.99.100:9092'
  producer:
   acks: 1
   key-serializer: org.apache.kafka.common.serialization.StringSerializer
   value-serializer: org.apache.kafka.common.serialization.StringSerializer
  consumer:
   enable-auto-commit: false
   group-id: test
   value-deserializer: org.apache.kafka.common.serialization.StringDeserializer
   key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
  listener:
   ## 手动确认消费
   ack-mode: manual

 ## redis
 redis:
  host: 127.0.0.1
  port: 6379
#  cluster:
#   nodes:
#   - 127.0.0.1:6379
#   - xxx.x.x.x:xxxx
#  password: cheney@redis
  timeout: 100000ms
  jedis:
   pool:
    max-active: 1024
    max-idle: 200
    max-wait: 10000ms
  database: 0
 #rabbitmq
 rabbitmq:
  host:

#############################################################

##other config
## redis config
redis:
 throwRedisException: true

## mongodb config
mongodb:
 auth: false
 authBase: admin
 host: 127.0.0.1
 port:
 database: blog
 username: root
 password: cheney@mongo

toolbox:
 entity-cache:
  enable: true
  type: REDIS
  underline: true
  entityPaths: com.cheney.entity
  autoRefresh: true
  period: 1


