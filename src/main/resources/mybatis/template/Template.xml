<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- namespace命名空间对应要映射的dao接口的全限定名 -->
<mapper namespace="@{namespace}">

    <sql id="filters">
        <where>
            <foreach collection="filters" item="filter" separator=",">
                <if test="filter.symbol != 'is null' and filter.symbol != 'is not null' and filter.symbol != 'in'">
                    and ${filter.property} ${filter.symbol} #{filter.value}
                </if>
                <if test="filter.symbol == 'is null' or filter.symbol == 'is not null'">
                    and ${filter.property} ${filter.symbol}
                </if>
                <if test="filter.symbol == 'in'">
                    ${filter.property}
                    in
                    <foreach collection="filter.value" item="item" separator="," open="(" close=")">
                        #{item}
                    </foreach>
                </if>
            </foreach>
        </where>
    </sql>

    <sql id="pageFilters">
        <where>
            <foreach collection="pageable.filters" item="filter" separator=",">
                <if test="filter.symbol != 'is null' and filter.symbol != 'is not null' and filter.symbol != 'in'">
                    and ${filter.property} ${filter.symbol} #{filter.value}
                </if>
                <if test="filter.symbol == 'is null' or filter.symbol == 'is not null'">
                    and ${filter.property} ${filter.symbol}
                </if>
                <if test="filter.symbol == 'in'">
                    ${filter.property}
                    in
                    <foreach collection="filter.value" item="item" separator="," open="(" close=")">
                        #{item}
                    </foreach>
                </if>
            </foreach>
        </where>
    </sql>

    <select id="findById" parameterType="@{idType}" resultType="@{package}">
        select *
        from @{tableName}
        where @{idColumn} = #{id}
    </select>

    <select id="findAll" resultType="@{package}">
        select *
        from @{tableName}
    </select>

    <insert id="persist" parameterType="@{package}">
        insert into @{tableName} (@{cloumns})
        values(@{columnValues})
    </insert>

    <update id="merge" parameterType="@{package}">
        update auth_user
        set @{setValue}
        where @{idColumn} = #{id}
    </update>

    <delete id="remove" parameterType="@{idType}">
        delete from @{tableName} where @{idColumn} = #{id}
    </delete>

    <delete id="multiRemove" parameterType="@{idType}">
        delete from @{tableName}
        where @{idColumn} in
        <foreach item="id" collection="ids" open="(" close=")" separator=",">
            #{id}
        </foreach>
    </delete>

    <select id="findPage" parameterType="com.cheney.system.page.Pageable" resultType="@{package}">
        select *
        from @{tableName}
        <include refid="pageFilters"/>
        limit #{pageable.startSize},#{pageable.pageSize}
    </select>

    <select id="findList" resultType="@{package}">
        select *
        from @{tableName}
        <include refid="filters"/>
    </select>

</mapper>