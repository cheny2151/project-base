package @{servicePackage};

@{serviceImportImpl};
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;

import javax.annotation.Resource;

/**
 * @{simpleClassName} - serviceImpl
 */
@Service("@{lowSimpleClassName}ServiceImpl")
@Transactional
public class @{simpleClassName}ServiceImpl extends BaseServiceImpl<@{simpleClassName}, @{idSimpleClassName}> implements @{simpleClassName}Service {

    @Resource
    private @{simpleClassName}Mapper @{lowSimpleClassName}Mapper;

    @Autowired
    public @{simpleClassName}ServiceImpl(@{simpleClassName}Mapper @{lowSimpleClassName}Mapper) {
        super(@{lowSimpleClassName}Mapper);
    }

}
